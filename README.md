## PyInstaller, but with a GUI

This project is meant to be a wrapper of the original CLI-based pyinstaller. 

While GUI wrappers for pyinstaller already exist, none of them is simple enough for the non-technical user since they all try to cover every feature of the CLI version.

